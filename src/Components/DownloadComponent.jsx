import * as React from "react";

const DownloadComponent = (props) => {
  const { vm } = props;
  return (
    <>
      &nbsp;(
      <a href={vm}>apply link</a>)
    </>
  );
};

export default DownloadComponent;
